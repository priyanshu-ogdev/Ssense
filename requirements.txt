# ── Core ML stack (CUDA 12.4) ──
# NOTE: We use the cu124 index. CUDA 12.4 is fully backward compatible with your DGX 12.1 drivers.
--extra-index-url https://download.pytorch.org/whl/cu124

numpy==1.26.4
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
xformers==0.0.28.post1

# ── Training & Alignment (Unsloth / TRL) ──
unsloth
trl==0.12.2
datasets==3.2.0
transformers==4.46.3
accelerate==1.2.1
peft==0.14.0
bitsandbytes==0.45.0

# ── Heavy Inference / Tree Generation (72B Model) ──
vllm==0.6.4.post1

# ── Local GGUF Evaluation (9B Model) ──
llama-cpp-python==0.3.6
jsonschema==4.23.0

# ── Scraping & Extraction ──
curl_cffi==0.7.4
playwright==1.51.0
playwright-stealth==1.0.6
readability-lxml==0.8.1
beautifulsoup4==4.12.3
tqdm==4.66.6
aiohttp==3.11.12
PyMuPDF==1.25.3

# ── Utilities ──
requests==2.32.3
tenacity==9.0.0